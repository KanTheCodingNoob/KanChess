{
  "name": "kan_chess_fullstack",
  "private": true,
  "scripts": {
    "frontend": "npm run dev --prefix kan_chess_frontend",
    "backend": "cd KanChess && mvnw.cmd spring-boot:run",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
